# =====================================================================
# أداة الملاحظات الذكية v2.0 — ملف المتطلبات
# Smart Remarks Tool v2.0 — Requirements File
# =====================================================================
# تثبيت: pip install -r requirements.txt
# Install: pip install -r requirements.txt
# =====================================================================

# -------- واجهة المستخدم (UI Framework) --------
streamlit>=1.32.0

# -------- معالجة ملفات الإكسيل (Excel Processing) --------
openpyxl>=3.1.2

# -------- تحليل البيانات (Data Analysis) --------
pandas>=2.2.0
numpy>=1.26.0

# -------- الرسوم البيانية التفاعلية (Interactive Charts) --------
plotly>=5.20.0

# -------- مكتبات Python القياسية المستخدمة (Standard Library) --------
# لا تحتاج تثبيتاً — مدمجة مع Python
# sys, io, re, logging, unicodedata, json, hashlib, base64, datetime
